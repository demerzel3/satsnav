import Foundation
import KrakenAPI

// private let apiKey = ProcessInfo.processInfo.environment["KRAKEN_API_KEY"]!
// private let privateKey = ProcessInfo.processInfo.environment["KRAKEN_API_SECRET"]!


// private let credentials = Kraken.Credentials(apiKey: apiKey, privateKey: privateKey)
// private let client = Kraken(credentials: credentials)
//
// print(Date().timeIntervalSinceReferenceDate * 1000)
//
// private let ledgersResult = await client.ledgersInfo()
// switch ledgersResult {
//    case .success(let ledgers):
//        print(ledgers)
//    case .failure(let error):
//        print(error)
// }
//
// private let tradesResult = await client.tradesHistory()
// switch tradesResult {
//    case .success(let trades):
//        print(trades)
//    case .failure(let error):
//        print(error)
// }
